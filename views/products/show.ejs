<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href= "/public/assets/styles.css" rel="stylesheet" type="text/css"/>
    <title><%= item.name %></title>
</head>
<body>
    <h1><%= item.name %></h1>
    <p>Price: <%=item.price%></p>
    <img src = <%= item.image %>>
    <p>Available: <%=item.amount%></p>


    <% if (cartObject.length > 0) { %>
        
        <div>
        <form action ="/products/cart/<%=cartObject[0]._id%>?_method=PUT" method="POST">
            <input class="hide" type = "number" name="amount" id="amount"value="<%=cartObject[0].amount+1%>"/>
            <button type = "submit">+</button>
        </form>
        <p>In Cart: <%=cartObject[0].amount%></p>
        <% if (cartObject[0].amount === 1) { %>
            <form action ="/products/cart/<%=cartObject[0]._id%>?_method=DELETE" method="POST">
                <button type = "submit">-</button>
            </form>
            <% } else { %>
        <form action ="/products/cart/<%=cartObject[0]._id%>?_method=PUT" method="POST">
            <input class="hide" type = "number" name="amount" id="amount"value="<%=cartObject[0].amount-1%>"/>
            <button type = "submit">-</button>
        </form>
        <% } %>
    </div>
   <%} else {%>
    <form action ="/products/cart" method="POST">
        <input class="hide" type ="text" name="name" id="name" value="<%= item.name %>"/>
        <input class="hide" type ="text" name="productID" id="productID" value="<%= item._id %>"/>
        <input class="hide" type ="text" name="category" id="category"value="<%= item.category %>"/>
        <input class="hide" type ="number" name="price" id="price"value="<%= item.price %>"/>
        <input class="hide" type ="text" name="image" id="image"value="<%= item.image %>"/>
        <input class="hide" type = "number" name="amount" id="amount"value="1"/>
        <button type = "submit">Add to Cart</button>
    </form>
    <%} %>
    <a href="/products">Back to Products</a>  
    <br>
    <a href="/cart">View Your Cart</a>  
</body>


</form>
</html>

